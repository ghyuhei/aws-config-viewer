# AWS Configuration
AWS_REGION=ap-northeast-1

# AWS Config Aggregator Name (required for cross-account queries)
CONFIG_AGGREGATOR_NAME=your-aggregator-name

# Optional: For local development with SSO
# AWS_PROFILE=your-sso-profile

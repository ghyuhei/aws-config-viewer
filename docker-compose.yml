services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - AWS_REGION=${AWS_REGION:-ap-northeast-1}
      - CONFIG_AGGREGATOR_NAME=${CONFIG_AGGREGATOR_NAME}
      - AWS_PROFILE=${AWS_PROFILE:-default}
    volumes:
      # Mount AWS credentials for local development with SSO
      # This allows using AWS SSO credentials from the host machine
      - ${HOME}/.aws:/home/nextjs/.aws:ro
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true

  # Development mode with hot reload
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    environment:
      - AWS_REGION=${AWS_REGION:-ap-northeast-1}
      - CONFIG_AGGREGATOR_NAME=${CONFIG_AGGREGATOR_NAME}
      - AWS_PROFILE=${AWS_PROFILE:-default}
    volumes:
      - .:/app
      - /app/node_modules
      - ${HOME}/.aws:/root/.aws:ro
